apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
    method: fromcode
    otherlabel: testlabel
  name: nginx-yaml
spec:
  containers:
  - image: nginx
    name: nginx
    ports:
    - containerPort: 80
